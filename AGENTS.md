# AGENTS.md

## 文档约定

- 本项目中由 Codex 产出的正式文档，一律使用中文文件名。
- 本项目中由 Codex 产出的正式文档，一律使用中文内容。
- 面向项目交付的设计稿、计划、规格、角色设定、关卡文档、原型文档、汇总文档，默认都用中文撰写。
- 如果确实需要英文，只能在用户明确要求时使用。

## 命名约定

- 文档文件名优先使用简洁中文，例如：`高概念.md`、`战斗支柱.md`、`章节节拍表.md`。
- 放在 `docs/plans` 下的日期文档，使用 `YYYY-MM-DD-中文主题.md` 形式。

## 沟通约定

- 与用户讨论文档内容时，默认使用中文。
- 新建文档前，先检查命名和语言是否符合本文件约定。
- 当用户在多选讨论中给出选择后，助手需要同步给出自己的判断，包括该选择的优点、风险、代价，以及可行的优化方案，不能只机械记录结果。
- 讨论战斗、操作、技能、招式等内容时，默认把“按键设计与输入流畅度”一起纳入讨论；招式设计不能脱离按键与操作节奏单独悬空。
- 讨论操作与按键时，默认以 Xbox 手柄键位表述为主，除非用户明确要求切换到键鼠或其他手柄方案。

## 对话记录约定

- 从 2026-03-16 起，本项目后续讨论默认同步保存在项目根目录下的 `Memery` 文件夹中。
- 记录内容默认保存用户可见对话细节，包含用户原话、助手给出的选项、以及用户的选择或回答。
- 记录文件使用时间戳命名。
- 每次对话都要按当前规则写入对应的对话记录，不跳过。
- 单个对话记录文件超过 `1500` 行后，不再继续追加，改为创建新的时间戳文件。

## 文档同步约定

- 每当与用户确认一个设计决定后，需要在当前讨论主题对应的正式文档中同步收束。
- 如果当前讨论仍在发散或分支过多，先在对话中完成收束，再把已确认内容整理进对应文档。
- 当前主题如果是战斗、剧情、角色、关卡、美术等具体文档，优先同步到 `docs/第一版文档` 下对应文件。
- 同步文档时只写入已经确认的结论，不把尚未确认的备选方案混入正式文档。
- 收束正式文档时，默认保留讨论中已经打准的关键表达、判断逻辑和设计语气，不把内容压缩成失去味道的纯摘要。
- 如果用户明确要求“贴近原话整理”，则优先沿用对话中的成熟表述，再做结构整理和必要精简。
- 所有新增细化内容，默认先走 `/brain` 式讨论与确认，再写入正式文档；不能把“尚未逐段讨论确认”的内容直接写进正式文档。
- 如果某一轮已经提前写入了尚未逐段确认的内容，需要明确标记为“待讨论确认”，并在后续按讨论结果修订或删除。
